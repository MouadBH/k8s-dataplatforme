[tool.poetry]
name = "test-pipline"
version = "0.1.0"
description = "Simple pyspark pipeline."
authors = ["MouadBH <mouad123b@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
